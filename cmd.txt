// For local build

npm run dist:win
npm run dist:mac:native

// For GitHub workflows
// prefix win winpty if using gitbash

// run the worklow
gh workflow run build-windows.yml

// or
gh workflow run build-macos.yml

// to monitor for completion
winpty gh run watch

// when finished download
winpty gh run download -D ./release


// clear old workflow runs, gets a list of runs and pipes into delete
gh run list --limit 100 --json databaseId -q '.[].databaseId' | xargs -n1 gh run delete
